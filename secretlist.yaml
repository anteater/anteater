secrets:
  file_names: [.gpg,.key,.asc,.sig,aws_access_key_id,aws_secret_access_key]
  file_contents: [-----begin\S*rsa\S*private\S*key-----,password,secret,ssh_key,private_key]


# For waivers, please use as full a string as possible
waivers:
  insecure-python: []
